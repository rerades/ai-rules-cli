# NPM Configuration for AI Rules CLI
# This file contains npm configuration for publishing

# Ensure proper package publishing
registry=https://registry.npmjs.org/

# Access level for scoped packages (if needed)
# access=public

# Save exact versions
save-exact=true

# Don't create package-lock.json during publish
package-lock=false
